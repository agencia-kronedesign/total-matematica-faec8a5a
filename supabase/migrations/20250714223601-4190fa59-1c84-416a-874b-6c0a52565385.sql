-- Vamos usar IDs temporários que depois serão atualizados via interface admin
-- Por enquanto, apenas transferir as atividades para um admin que pode atuar como professor

-- Verificar atividades atuais
SELECT id, titulo, professor_id FROM atividades;